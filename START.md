# 🚀 Инструкция по запуску проекта

## Что было сделано

Ваш сайт обновлен на современный стек с **Eleventy** + **Alpine.js**:

✅ **Улучшен дизайн** - CSS переменные, design system, микроанимации
✅ **Шаблоны** - Нет дублирования кода, header/footer в компонентах
✅ **Интерактивные тесты** - Alpine.js вместо vanilla JS
✅ **Формы** - Улучшенная валидация и стили для login/register
✅ **Оптимизация** - Скрипт конвертации PNG → WebP
✅ **Документация** - Подробный README и инструкции для разработки

## 📦 Установка

### 1. Установите Node.js

Если еще не установлен, скачайте с https://nodejs.org/ (версия 18+)

Проверьте установку:
```bash
node --version
npm --version
```

### 2. Установите зависимости

Откройте PowerShell в папке проекта:

```powershell
cd C:\Users\Ak417ytrfg\Documents\linear-programming-course
npm install
```

Это установит:
- Eleventy (генератор сайта)
- Alpine.js (интерактивность)
- Sharp (оптимизация изображений)

### 3. Запустите сервер разработки

```powershell
npm run dev
```

Автоматически откроется браузер на `http://localhost:8080`

✨ **Hot Reload** работает - при изменении файлов страница обновляется автоматически!

## 📂 Новая структура файлов

### ⚠️ ВАЖНО: Старые файлы НЕ трогайте!

Новая структура в папке `src/`:
```
src/
├── _includes/          # Шаблоны (header, footer, layouts)
├── _data/              # Данные (quizData.json)
├── css/                # style.css с CSS переменными
├── js/                 # quiz.js + quiz-data.json
├── assets/images/      # Оптимизированные изображения (после npm run optimize-images)
├── index.njk           # Главная
├── test.njk            # Тесты
├── login.njk           # Вход
├── register.njk        # Регистрация
└── ... (другие страницы - нужно будет создать)
```

Старые файлы остались в корне:
```
index.html, test.html, login.html и т.д. - можно удалить после миграции
style.css - старый файл, теперь используется src/css/style.css
assets/ - оригинальные PNG, можно оставить для резерва
```

## 🎨 Что изменилось

### 1. Дизайн
- ✨ **CSS переменные** для цветов, отступов, шрифтов
- 🎭 **Микроанимации** на кнопках и карточках
- 📱 **Улучшенная адаптивность**
- 🎨 **Единая дизайн-система**

### 2. Формы
- ✅ Красивые стилизованные input/button
- ✅ Валидация на клиенте
- ✅ Placeholders и autocomplete
- ⚠️ Пока без backend (заглушки с сообщениями)

### 3. Тесты
- 🔥 Переписаны на **Alpine.js** (читабельнее, меньше кода)
- 📊 Данные вынесены в JSON (`src/js/quiz-data.json`)
- 🎯 Плавные переходы между экранами
- ♿ Улучшенная accessibility

## 🛠️ Команды для разработки

```powershell
# Разработка (с live reload)
npm run dev

# Сборка для продакшна (создает папку dist/)
npm run build

# Оптимизация изображений (PNG → WebP)
npm run optimize-images
```

## 📝 Как работать дальше

### Добавить новую страницу

1. Создайте файл `src/mypage.njk`:
```njk
---
layout: base.njk
title: Моя страница
description: Описание для SEO
---

<section class="content fade-in">
    <h2>Заголовок</h2>
    <p>Контент страницы</p>
</section>
```

2. Добавьте ссылку в `src/_includes/header.njk`

3. Сохраните - Eleventy автоматически создаст `/mypage/`

### Добавить вопросы в тест

Редактируйте `src/js/quiz-data.json`:

```json
{
  "новая-тема": {
    "title": "Тест: Новая тема",
    "questions": [
      {
        "question": "Ваш вопрос?",
        "options": ["Вариант 1", "Вариант 2", "Вариант 3"],
        "correctAnswer": 0
      }
    ]
  }
}
```

### Изменить дизайн

Редактируйте `src/css/style.css`. Используйте CSS переменные:

```css
/* Изменить основной цвет */
:root {
  --color-primary-500: #007bff; /* Было */
  --color-primary-500: #0066cc; /* Стало */
}

/* Новый компонент */
.my-component {
  padding: var(--space-lg);
  color: var(--color-primary-500);
  border-radius: var(--border-radius-md);
}
```

## 🔄 Миграция старых страниц

Пока созданы только основные страницы:
- ✅ index.njk (главная)
- ✅ test.njk (тесты)
- ✅ login.njk (вход)
- ✅ register.njk (регистрация)

**Нужно создать:**
- ⏳ basics.njk (основные понятия)
- ⏳ simplex.njk (симплекс-метод)
- ⏳ duality.njk (двойственность)
- ⏳ glossary.njk (глоссарий)
- ⏳ literature.njk (литература)

Я могу помочь с миграцией контента из старых HTML файлов!

## 📚 Полезные ссылки

- [Eleventy документация](https://www.11ty.dev/docs/)
- [Alpine.js документация](https://alpinejs.dev/)
- [CSS Custom Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties)

## 🆘 Помощь

### Ошибка при npm install

```powershell
# Очистите кэш и попробуйте снова
npm cache clean --force
npm install
```

### Порт 8080 занят

Измените в `.eleventy.js`:
```js
return {
  // ... остальное
  serverOptions: {
    port: 3000  // Другой порт
  }
};
```

### Изменения не применяются

1. Остановите сервер (Ctrl+C)
2. Удалите папку `dist/`
3. Запустите снова `npm run dev`

## ✅ Следующие шаги

1. ✅ Запустите `npm install`
2. ✅ Запустите `npm run dev`
3. ✅ Откройте http://localhost:8080
4. ⏳ Мигрируйте оставшиеся страницы из HTML в .njk
5. ⏳ Запустите `npm run optimize-images` для конвертации изображений

**Готово! 🎉** Теперь у вас современный, масштабируемый сайт с удобной разработкой!

Если нужна помощь с миграцией контента или добавлением новых фич - спрашивайте!
