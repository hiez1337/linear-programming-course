# 📚 Курс "Линейное программирование"

Обучающий сайт по линейному программированию на русском языке с интерактивными тестами и математическим контентом.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Разработка

```bash
npm run dev
```

Откроется браузер на `http://localhost:8080` с hot-reload.

### Сборка для продакшна

```bash
npm run build
```

Результат будет в папке `dist/`.

## 📁 Структура проекта

```
linear-programming-course/
├── src/                          # Исходники
│   ├── _includes/                # Шаблоны Nunjucks
│   │   ├── base.njk              # Базовый layout
│   │   ├── header.njk            # Шапка сайта
│   │   ├── footer.njk            # Подвал
│   │   └── content-page.njk      # Layout для контентных страниц
│   ├── _data/                    # Данные для шаблонов
│   │   └── quizData.json         # Данные тестов
│   ├── css/                      # Стили
│   │   └── style.css             # Единый CSS с переменными
│   ├── js/                       # JavaScript
│   │   ├── quiz.js               # Логика тестов (Alpine.js)
│   │   └── quiz-data.json        # JSON с вопросами
│   ├── assets/                   # Статические файлы
│   │   └── images/               # Оптимизированные изображения
│   ├── index.njk                 # Главная страница
│   ├── test.njk                  # Страница тестов
│   ├── login.njk                 # Вход (заглушка)
│   ├── register.njk              # Регистрация (заглушка)
│   └── ... (другие страницы)
├── assets/                       # Оригинальные изображения
├── scripts/                      # Утилиты
│   └── optimize-images.js        # Скрипт оптимизации изображений
├── dist/                         # Собранный сайт (git ignore)
├── .eleventy.js                  # Конфигурация Eleventy
├── package.json
└── README.md
```

## 🛠️ Технологический стек

### Core
- **Eleventy (11ty)** - Static Site Generator
- **Nunjucks** - Шаблонизатор
- **Alpine.js** - Реактивность для тестов
- **MathJax 3** - Рендеринг математических формул

### CSS
- **CSS Custom Properties** - Design System с переменными
- **Flexbox & Grid** - Современная верстка
- **Mobile-First** - Адаптивный дизайн

### Оптимизация
- **Sharp** - Конвертация PNG → WebP
- **PostCSS** (опционально) - Autoprefixer, minification

## 📝 Работа с контентом

### Добавление нового раздела с математикой

1. Создайте файл `src/new-section.njk`:

```njk
---
layout: content-page.njk
title: Название раздела
breadcrumb: Название раздела
includeMathJax: true
sidebarItems:
  - id: section1
    title: Подраздел 1
  - id: section2
    title: Подраздел 2
---

<h2>Глава X. Название</h2>

<h3 id="section1">Подраздел 1</h3>
<p>Текст с формулой: $x^2 + y^2 = z^2$</p>

<h3 id="section2">Подраздел 2</h3>
<p>$$\begin{aligned}
  ax + by &= c \\
  dx + ey &= f
\end{aligned}$$</p>

<div class="navigation-links">
    <a href="/previous/">← Предыдущая тема</a>
    <a href="/next/">Следующая тема →</a>
</div>
```

### Добавление вопросов в тест

Отредактируйте `src/js/quiz-data.json`:

```json
{
  "new-topic": {
    "title": "Тест: Новая тема",
    "questions": [
      {
        "question": "Вопрос?",
        "options": ["Вариант 1", "Вариант 2", "Вариант 3"],
        "correctAnswer": 0
      }
    ]
  }
}
```

## 🎨 Design System

### Цветовая палитра

```css
:root {
  --color-primary-500: #007bff;    /* Основной синий */
  --color-success: #28a745;         /* Зеленый (успех) */
  --color-error: #dc3545;           /* Красный (ошибка) */
  --color-gray-900: #343a40;        /* Темный текст */
}
```

### Spacing

```css
--space-xs: 0.25rem;    /* 4px */
--space-sm: 0.5rem;     /* 8px */
--space-md: 1rem;       /* 16px */
--space-lg: 1.5rem;     /* 24px */
--space-xl: 2rem;       /* 32px */
```

### Компоненты

- `.btn` - Кнопки (primary, secondary, cta)
- `.feature-item` - Карточки возможностей
- `.question` - Вопросы теста
- `.sidebar` - Боковое меню

## 🖼️ Оптимизация изображений

Конвертация PNG в WebP:

```bash
npm run optimize-images
```

Это создаст:
- `*.webp` - Оптимизированные версии
- Оригиналы как fallback

## 🔧 Разработка

### Добавление новой страницы

1. Создайте `src/page.njk` с front matter
2. Eleventy автоматически создаст `/page/` URL
3. Добавьте ссылку в `src/_includes/header.njk`

### Hot Reload

Eleventy отслеживает изменения в:
- `src/**/*.njk`
- `src/css/**/*.css`
- `src/js/**/*.js`

## 📦 Деплой

### GitHub Pages

```bash
npm run build
# Загрузите папку dist/ на gh-pages ветку
```

### Netlify / Vercel

Подключите репозиторий, настройки:
- Build command: `npm run build`
- Publish directory: `dist`

## ✨ Фичи

- ✅ Адаптивный дизайн (mobile-first)
- ✅ Accessibility (ARIA, keyboard navigation)
- ✅ SEO оптимизация
- ✅ Математические формулы (MathJax)
- ✅ Интерактивные тесты (Alpine.js)
- ✅ Оптимизация изображений (WebP)
- ✅ Design System (CSS Variables)
- 🚧 Авторизация (заглушка, без backend)

## 🔮 Планы развития

- [ ] Backend для авторизации (Supabase/Firebase)
- [ ] Сохранение прогресса пользователя
- [ ] История прохождения тестов
- [ ] Темная тема
- [ ] Печать страниц (print.css)
- [ ] Progressive Web App (PWA)

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с ❤️ для изучения линейного программирования
